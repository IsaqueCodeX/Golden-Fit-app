
import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.goldenfit.app',
  appName: 'Golden Fit',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
